# Build output. tools/build-dist.py assembles the publishable site here, and
# this is the only directory the hosting platform serves. It is rebuilt from
# the tracked sources on every deploy, so it is never committed.
dist/

# Internal pre-sales material.
docs/
deck/

# Local scratch
node_modules/
__pycache__/
.DS_Store
.preview/
